odoo.define('s_base.force_hide_filter_group_by', function (require) {
    "use strict";
    // var ControlPanelRenderer = require('web.ControlPanelRenderer');
    const session = require('web.session');
    // ControlPanelRenderer.include({
    //     init: function (parent, state, params) {
    //         this._super.apply(this, arguments);
    //         // start logan
    //         var self = this;
    //         if (self.action['xml_id']) {
    //             if (self.action['xml_id'].indexOf('force_hide_filter_group_by') !== -1 && !session.is_admin) {
    //                 self.displaySearchMenu = false
    //             }
    //         }
    //         // end logan
    //     },
    //     _onMore: function () {
    //
    //     },
    // });
    // return ControlPanelRenderer
});